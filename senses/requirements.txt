# Cosmos Senses — zależności
# Rdzeń usługi (wymagany):
fastapi
uvicorn
python-multipart

# Zmysły — instaluj te, których chcesz używać (każdy jest opcjonalny):

# SŁUCH — rozpoznawanie mowy (Whisper); świetnie zna polski
faster-whisper

# GŁOS — synteza mowy (Piper); pobierz też plik głosu .onnx, np.
# https://huggingface.co/rhasspy/piper-voices (pl_PL-darkman-medium)
piper-tts

# WZROK — detekcja obiektów (YOLO) + obraz
ultralytics
opencv-python

# CIAŁO — sylwetka i pozy (MediaPipe)
mediapipe

# PAMIĘĆ — embeddingi do pamięci długotrwałej (RAG)
# bge-m3 (~2 GB, domyślny) lub lżejszy: ustaw EMBED_MODEL=paraphrase-multilingual-MiniLM-L12-v2
sentence-transformers

# Obserwatory (watcher.py, kinect_watcher.py, photoscan.py)
requests
numpy

# GŁĘBIA — Kinect 360 przez libfreenect (kinect_watcher.py):
# moduł "freenect" instaluje się razem z libfreenect — patrz senses/README.md
